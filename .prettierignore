node_modules
dist
build
.expo
*.tsbuildinfo

# electron-vite bundle output (apps/desktop/out) — generated, never authored.
apps/desktop/out

# Pre-existing governance docs — owned by the docs loop, not reformatted here.
*.md
docs/
.github/

# Expo template — kept in the template's own style, not reformatted here.
apps/mobile/
